import { FButtonDirective } from './f-button.directive';

describe('FButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FButtonDirective();
    expect(directive).toBeTruthy();
  });
});
